# PhysioNet credentials — copy to .env and fill in (NEVER commit .env)
PHYSIONET_USER=your_physionet_username
PHYSIONET_PASS=your_physionet_password

# Optional: override data root (default: see configs/paths.yaml)
# MORE_DATA_ROOT=/scratch/user/dshekar/more-clinical-distill/data
